# Campaign 0001 controls

These manifests define the first method-neutral control treatments for the reproduction/falsification campaign.

They are configuration scaffolds, not evidence of completed runs.

## Treatments

- `direct.json` — no explicit reasoning treatment.
- `textual-cot.json` — explicit natural-language chain-of-thought control.
- `serial-control.json` — extra serial/recurrent computation without a latent-representation claim.

The serial-control depth is provisional. WP2 must calibrate a meaningful compute match before any causal comparison is promoted.

All backbone revisions and Foundry references remain unresolved until the exact model artifact is admitted/pinned.
